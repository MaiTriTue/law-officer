export interface IInfoBase {
    id: string;
    image: string;
    name: string;
    position: string; // vị trí
    field: Array<string>; // lĩnh vực
    experience: number; // kinh nghiệm
    level: string; // trình độ
    phone: string; // sdt
    numberOfPost: number;
}
